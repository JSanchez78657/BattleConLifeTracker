<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <application
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/battle_con_assistant"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/battle_con_assistant"
            android:theme="@style/Theme.BattleConLifeTracker"
            android:supportsRtl="true"
            tools:targetApi="31">
        <activity
                android:name=".layouts.MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".layouts.SpinnerLayout" android:exported="true"/>
        <activity android:name=".layouts.DynamicGameScreen" android:exported="true" android:configChanges="orientation|screenSize"/>
        <activity android:name=".layouts.SettingsMenu" android:exported="true" android:configChanges="orientation|screenSize"/>
    </application>

</manifest>